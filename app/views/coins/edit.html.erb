<h1><%= t('links.edit') %></h1>

<%= render 'form', coin: @coin %>

<%= link_to t('links.show'), @coin, type:"button", class:"btn btn-primary" %>
<%= link_to t('links.back'), coins_path, type:"button", class:"btn btn-primary" %>
